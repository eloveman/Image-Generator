# Image-Generator

How the code works
Using an API we generate the image prompt we used the model, "gpt-3.5-turbo-instruct"
Then we generate the image based on the image prompt using model, dall-e-2'
Than we have the main function were the story is generated from the users prompts. 
